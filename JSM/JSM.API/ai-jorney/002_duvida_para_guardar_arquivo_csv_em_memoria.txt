primeiro passo desenvolver uma API Controller em C# usando .NetCore 8.0 que le um arquivo CSV em anexo e guarda em memoria.

segundo passo desenvolver uma API Controller em C# usando .NetCore 8.0 que le o arquivo CSV em anexo em memoria e filtra por (region, type,pageNumber,pageSize)